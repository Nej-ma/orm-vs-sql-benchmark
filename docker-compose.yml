version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: ads_postgres
    environment:
      POSTGRES_USER: ads
      POSTGRES_PASSWORD: ads_secret
      POSTGRES_DB: ads_benchmark
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ads -d ads_benchmark"]
      interval: 5s
      timeout: 5s
      retries: 10

  sql-api:
    build:
      context: ./sql-api
      dockerfile: Dockerfile
    container_name: ads_sql_api
    environment:
      NODE_ENV: production
      PORT: 3001
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ads
      DB_PASSWORD: ads_secret
      DB_NAME: ads_benchmark
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  orm-api:
    build:
      context: ./orm-api
      dockerfile: Dockerfile
    container_name: ads_orm_api
    environment:
      NODE_ENV: production
      PORT: 3002
      DATABASE_URL: "postgresql://ads:ads_secret@postgres:5432/ads_benchmark"
    ports:
      - "3002:3002"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  seeder:
    build:
      context: ./seed
      dockerfile: Dockerfile
    container_name: ads_seeder
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: ads
      DB_PASSWORD: ads_secret
      DB_NAME: ads_benchmark
    depends_on:
      postgres:
        condition: service_healthy
    profiles:
      - seed

  # k6 load testing — runs on the same Docker network as the APIs
  # Usage: docker compose run --rm k6 run --env BASE_URL=http://sql-api:3001 /scripts/scenarios/01-smoke.js
  k6:
    image: grafana/k6:latest
    container_name: ads_k6
    volumes:
      - ./k6:/scripts
      - ./results:/results
    depends_on:
      - sql-api
      - orm-api
    profiles:
      - k6

volumes:
  postgres_data:
